# include <stdio.h>

int main()
{
    int i,t=0;
    for(i=1;i<100;i++)
    {
        if(i%13 == 0)
        {
            t=t+i;
        }
    }
    printf("Sum if all the numbers divisible by 13 from 1-100 is : %d",t);
    
}
